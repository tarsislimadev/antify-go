FROM node:lts-alpine

# USER node

# ENV ANTIFY_ROOT_PASSWORD=root

ENV ANTIFY_PATH=/data

ENV ANTIFY_HTTP_PORT=80

# ENV ANTIFY_WS_PORT=87

# ENV ANTIFY_NET_PORT=22

# ENV ANTIFY_DATA_PATH=/data

WORKDIR /app

COPY . .

CMD npm start
